# Project Structure

- Frontend app is in the repository root (`src`, `public`, `vite.config.js`, etc.).
- Backend API is in `backend/`.

## Run

From repository root:

```bash
npm run dev
```

This starts Vite (frontend) and `backend` (API) together.
